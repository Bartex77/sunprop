{% extends 'base.html.twig' %}

{% block title %}Amenity{% endblock %}

{% block body %}
    <h1>Amenity</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ amenity.id }}</td>
            </tr>
            <tr>
                <th>Name [en]</th>
                <td>{{ amenity.translate('en').name }}</td>
            </tr>
            <tr>
                <th>Name [pl]</th>
                <td>{{ amenity.translate('pl').name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('amenity_index') }}">back to list</a>

    <a href="{{ path('amenity_edit', {'id': amenity.id}) }}">edit</a>

    {{ include('amenity/_delete_form.html.twig') }}
{% endblock %}
