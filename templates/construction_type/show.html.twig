{% extends 'base.html.twig' %}

{% block title %}ConstructionType{% endblock %}

{% block body %}
    <h1>ConstructionType</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ construction_type.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ construction_type.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('construction_type_index') }}">back to list</a>

    <a href="{{ path('construction_type_edit', {'id': construction_type.id}) }}">edit</a>

    {{ include('construction_type/_delete_form.html.twig') }}
{% endblock %}
